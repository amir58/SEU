<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amirmohammed.seu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoBar">
        <activity android:name=".PatientDetailsActivity"/>
        <activity android:name=".PatientActivity" />
        <activity android:name=".CampaignsActivity" />
        <activity android:name=".DonorDateAndTimeActivity" />
        <activity android:name=".CampaignsDateAndTimeActivity" />
        <activity android:name=".DonorTypeActivity" />
        <activity android:name=".QuestionnaireActivity" />
        <activity android:name=".DonorDataActivity" />
        <activity android:name=".SignUpCodeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".deprecated.LoginCodeActivity" />
        <activity android:name=".deprecated.LoginActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"
            android:label="@string/app_name"/>

        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppThemeNoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>